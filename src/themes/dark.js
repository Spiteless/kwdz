import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: "#0096FF",
    },
    secondary: {
      main: "#ff9100",
    },
  },
}
);



export default theme;